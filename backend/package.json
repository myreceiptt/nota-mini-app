{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:receipt": "hardhat run scripts/deploy-receipt.ts --network baseMainnet",
    "deploy:nft": "hardhat run scripts/deploy-nft.ts --network baseSepolia",
    "deploy:all": "hardhat run scripts/deploy-all.ts --network baseSepolia",
    "deploy:deterministic": "hardhat run scripts/deploy-deterministic.ts --network baseSepolia",
    "verify:receipt": "hardhat verify --network baseMainnet",
    "verify:nft": "hardhat verify --network baseSepolia",
    "manual:stamp": "hardhat run scripts/manual-stamp.ts --network baseMainnet",
    "manual:transfer": "hardhat run scripts/manual-transfer.ts --network baseMainnet",
    "manual:mint": "hardhat run scripts/manual-mint.ts --network baseSepolia",
    "read:receipt": "hardhat run scripts/read-receipt.ts --network baseMainnet",
    "stamp:auto:once": "hardhat run scripts/auto-stamp-once.ts --network baseMainnet",
    "stamp:auto:batch": "hardhat run scripts/auto-stamp-batch.ts --network baseMainnet",
    "stamp:auto:stop": "node scripts/auto-stamp-stop.mjs",
    "deploy:receipt:sepolia": "hardhat run scripts/deploy-receipt.ts --network baseSepolia",
    "verify:receipt:sepolia": "hardhat verify --network baseSepolia",
    "manual:stamp:sepolia": "hardhat run scripts/manual-stamp.ts --network baseSepolia",
    "manual:transfer:sepolia": "hardhat run scripts/manual-transfer.ts --network baseSepolia",
    "read:receipt:sepolia": "hardhat run scripts/read-receipt.ts --network baseSepolia"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=24.0.0"
  },
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/node": "^24.10.4",
    "hardhat": "^2.28.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3"
  }
}
